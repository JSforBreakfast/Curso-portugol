algoritmo "Somador"
var

cont, num, soma: inteiro

inicio

cont <- 1
soma <- 0
enquanto (cont <= 5) faca
escreva("Digite o", cont, "o valor: ")
leia(num)
soma <- soma + num
cont <- cont + 1
escreval("Asoma dos valores foi: ", soma)
fimenquanto
fimalgoritmo
